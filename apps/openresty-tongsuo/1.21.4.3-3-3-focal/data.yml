additionalProperties:
    formFields:
        - default: 80
          envKey: PANEL_APP_PORT_HTTP
          labelEn: HTTP Port
          required: true
          type: number
          label:
            en: HTTP Port
            es-es: Puerto HTTP
            ja: HTTP ポート
            ms: Port HTTP
            pt-br: Porta HTTP
            ru: HTTP порт
            ko: HTTP 포트
            tr: HTTP Portu
            zh-Hant: HTTP 埠
            zh: HTTP 端口
        - default: 443
          envKey: PANEL_APP_PORT_HTTPS
          labelEn: HTTPS Port
          required: true
          type: number
          label:
            en: HTTPS Port
            es-es: Puerto HTTPS
            ja: HTTPS ポート
            ms: Port HTTPS
            pt-br: Porta HTTPS
            ru: HTTPS порт
            ko: HTTPS 포트
            tr: HTTPS Portu
            zh-Hant: HTTPS 埠
            zh: HTTPS 端口
